import Foundation

// Intentionally retained as a small compatibility unit because the Xcode
// project already references this source path. The live main-chat transport
// selection now happens in
// ChatPanelView+PartN_RuntimeTransportSelection.swift.
